.pio/
.vscode/
# Generated locally via `pio run -t compiledb` so clangd can see PIO's
# include paths (Arduino.h, libdvi, Pico-PIO-USB, …) — not part of the repo.
compile_commands.json
.cache/
build/
*.o
*.elf
*.bin
*.uf2
.DS_Store

# local reference scratch (vendor demo zip + raw downloads; findings are documented in README.md/docs/)
.ref/

# Camera originals. The keepers live downscaled in hardware/case/photos/;
# 100 MB of phone PNGs does not belong in history.
hardware/photos/
